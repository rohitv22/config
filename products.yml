spring:
  profiles:
    active:
      - "qa"
  jpa:
    show-sql: true
  application:
    name: products
server:
  port: 8082
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
    info:
      enabled: true
build:
  version: "1.0"
info:
  message: "Welcome to busycoder product default profile"
  name: "Raj: Product Owner product default profile"

